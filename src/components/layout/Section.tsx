import styled from "styled-components";

const Section = styled.section`
    position: relative;
    
    width: 100vw;
    height: 100vh;
    
    flex-grow: 0;
    flex-shrink: 0;
`

export default Section;